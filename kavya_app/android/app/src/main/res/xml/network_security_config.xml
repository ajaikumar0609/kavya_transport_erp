<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for Kavya Transports (production).
  - Blocks all cleartext (HTTP) traffic — HTTPS only.
  - Allows cleartext to 10.0.2.2 and localhost for development/emulator use only.
  Reference: https://developer.android.com/training/articles/security-config
-->
<network-security-config>

    <!-- Production: HTTPS-only for the live API domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">kavyatransports.com</domain>
        <domain includeSubdomains="true">api.kavyatransports.com</domain>
    </domain-config>

    <!-- Development exception: allow HTTP to emulator host and localhost only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Default: block cleartext for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

</network-security-config>
